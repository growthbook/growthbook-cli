import { BaseApiRepository, BaseRepository } from './base.repository'
import { {{pascalCase resource}}sApi } from '../generated/api'

export class {{pascalCase resource}}sRepository extends BaseRepository implements BaseApiRepository<{{pascalCase resource}}sApi> {
  api(): {{pascalCase resource}}sApi  {
    return new {{pascalCase resource}}sApi(this.apiConfig())
  }
}
